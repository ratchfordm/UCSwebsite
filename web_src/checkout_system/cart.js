
function checkInputLength() {
    const itemId = document.getElementById("itemId").value;

    // if (itemId.length === 7) { // if the id is 7 long, reload the page
    //     window.location.href = "cart.php?";
    // }

}